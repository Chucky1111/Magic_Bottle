{
  "name": "107",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build game.ts --outfile game.js --target browser",
    "dev": "bun --watch game.ts --outfile game.js --target browser",
    "start": "bun run build && echo 'Open index.html in browser'"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
