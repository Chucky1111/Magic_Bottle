{
  "name": "108",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build ./src/main.ts --outdir public/js --target browser --minify",
    "dev": "bun build ./src/main.ts --outdir public/js --target browser --watch",
    "serve": "bun run server.ts",
    "start": "bun run build && bun run serve"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
